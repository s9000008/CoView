# Syncine 系統環境變數範例配置
# 請複製此檔案並重新命名為 .env 使用

# 後端伺服器通訊埠 (Port)
PORT=3000

# 預設官方連線伺服器網址 (前端 Extension 預設連線位置)
VITE_DEFAULT_SERVER_URL=https://coview.fly.dev

# 跨域請求 CORS 允許來源 (可使用逗號分隔多個網址)
CORS_ORIGIN=*

# 日誌等級 (debug | info | warn | error)
LOG_LEVEL=info
